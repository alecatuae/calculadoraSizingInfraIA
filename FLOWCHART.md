# 🔄 FLUXOGRAMA DO SISTEMA
## Sistema de Dimensionamento de Inferência LLM

---

## 📊 FLUXO PRINCIPAL

```
┌─────────────────────────────────────────────────────────────────┐
│                         ENTRADA DO USUÁRIO                       │
│                                                                  │
│  python3 sizing.py --model MODEL --server SERVER --storage ...  │
└───────────────────────────────┬─────────────────────────────────┘
                                │
                                ▼
┌─────────────────────────────────────────────────────────────────┐
│                     1. CARREGAR DADOS (JSON)                     │
│                                                                  │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │
│  │ models.json  │  │ servers.json │  │ storage.json │          │
│  └──────────────┘  └──────────────┘  └──────────────┘          │
│         │                  │                  │                 │
│         └──────────────────┼──────────────────┘                 │
│                            ▼                                     │
│              Model, Server, Storage objects                      │
└───────────────────────────────┬─────────────────────────────────┘
                                │
                                ▼
┌─────────────────────────────────────────────────────────────────┐
│             2. CALCULAR KV CACHE POR SESSÃO (GiB)               │
│                                                                  │
│  Fórmula: 2 × seq × num_kv_heads × head_dim × bytes            │
│                                                                  │
│  ┌──────────────────────────────────────────┐                  │
│  │  Padrão de Atenção:                      │                  │
│  │  • Full: seq = effective_context         │                  │
│  │  • Sliding: seq = sliding_window         │                  │
│  │  • Hybrid: mix de full + sliding         │                  │
│  └──────────────────────────────────────────┘                  │
│                            ▼                                     │
│                   KV_per_session_gib                            │
└───────────────────────────────┬─────────────────────────────────┘
                                │
                                ▼
┌─────────────────────────────────────────────────────────────────┐
│                3. CALCULAR KV TOTAL (TiB)                       │
│                                                                  │
│     KV_total = KV_per_session × concurrency                     │
└───────────────────────────────┬─────────────────────────────────┘
                                │
                                ▼
┌─────────────────────────────────────────────────────────────────┐
│             4. CALCULAR SESSÕES POR NÓ                          │
│                                                                  │
│  Budget_KV = (HBM_total × kv_budget_ratio) - runtime_overhead  │
│                                                                  │
│  Sessions_per_node = floor(Budget_KV / KV_per_session)         │
└───────────────────────────────┬─────────────────────────────────┘
                                │
                                ▼
┌─────────────────────────────────────────────────────────────────┐
│              5. CALCULAR NÚMERO DE NÓS                          │
│                                                                  │
│  Nodes_minimum = ceil(concurrency / sessions_per_node)         │
│                            │                                     │
│                            ▼                                     │
│  Nodes_with_headroom = ceil(concurrency × (1 + headroom)       │
│                            / sessions_per_node)                 │
│                            │                                     │
│                            ▼                                     │
│  Nodes_final = Nodes_with_headroom + (1 if ha=n+1)            │
└───────────────────────────────┬─────────────────────────────────┘
                                │
                                ▼
┌─────────────────────────────────────────────────────────────────┐
│            6. GERAR AVISOS E VALIDAÇÕES                         │
│                                                                  │
│  • Context > max_position_embeddings? → Clamp                  │
│  • KV precision = fp16/bf16? → Aviso (2x memória)              │
│  • Context > 128k? → Aviso (pressão I/O)                       │
│  • Storage cloud? → Aviso (latência)                           │
└───────────────────────────────┬─────────────────────────────────┘
                                │
                                ▼
┌─────────────────────────────────────────────────────────────────┐
│                     7. FORMATAR SAÍDA                           │
│                                                                  │
│  ┌──────────────────┐          ┌──────────────────┐            │
│  │  Relatório Texto │          │   JSON Output    │            │
│  │   (stdout)       │          │   (integração)   │            │
│  └──────────────────┘          └──────────────────┘            │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔍 DETALHAMENTO: CÁLCULO DE KV CACHE

```
┌─────────────────────────────────────────────────────────────────┐
│                  PADRÕES DE ATENÇÃO                             │
└─────────────────────────────────────────────────────────────────┘

┌──────────────────┐
│  FULL ATTENTION  │
└──────────────────┘
     │
     ▼
┌─────────────────────────────────────────┐
│ Todas as camadas:                       │
│   KV = 2 × effective_context × ...      │
│                                         │
│ Total = num_layers × KV_per_layer      │
└─────────────────────────────────────────┘

┌─────────────────────┐
│  SLIDING ATTENTION  │
└─────────────────────┘
     │
     ▼
┌─────────────────────────────────────────┐
│ Todas as camadas:                       │
│   KV = 2 × sliding_window × ...         │
│                                         │
│ Total = num_layers × KV_per_layer      │
└─────────────────────────────────────────┘

┌──────────────────┐
│  HYBRID (Misto)  │
└──────────────────┘
     │
     ▼
┌─────────────────────────────────────────┐
│ Full layers:                            │
│   KV_full = 2 × effective_context × ... │
│   Total_full = full_layers × KV_full   │
│                                         │
│ Sliding layers:                         │
│   KV_slide = 2 × sliding_window × ...   │
│   Total_slide = sliding_layers × ...    │
│                                         │
│ Total = Total_full + Total_slide        │
└─────────────────────────────────────────┘
```

---

## 🎯 DECISÕES DO SISTEMA

```
┌─────────────────────────────────────────────────────────────────┐
│                    ÁRVORE DE DECISÃO                            │
└─────────────────────────────────────────────────────────────────┘

effective_context > max_position_embeddings?
├── SIM: Clamp para max_position_embeddings + Aviso
└── NÃO: Usar effective_context

kv_precision = fp16 ou bf16?
├── SIM: Usar 2 bytes/elem + Aviso (considere fp8)
└── NÃO: Usar 1 byte/elem

sessions_per_node = 0?
├── SIM: ERRO - Budget HBM insuficiente
└── NÃO: Continuar

effective_context > 128000?
├── SIM: Gerar alerta de I/O storage
└── NÃO: OK

ha = "n+1"?
├── SIM: nodes_final = nodes_with_headroom + 1
└── NÃO: nodes_final = nodes_with_headroom
```

---

## 📦 ARQUITETURA DE DADOS

```
┌─────────────────────────────────────────────────────────────────┐
│                   ESTRUTURA DE DADOS                            │
└─────────────────────────────────────────────────────────────────┘

Model (Dataclass)
├── name: str
├── num_layers: int
├── num_key_value_heads: int
├── head_dim: int
├── max_position_embeddings: int
├── attention_pattern: str (full|sliding|hybrid)
├── hybrid_full_layers: Optional[int]
├── hybrid_sliding_layers: Optional[int]
├── sliding_window: Optional[int]
└── default_kv_precision: str

Server (Dataclass)
├── name: str
├── gpus: int
├── hbm_per_gpu_gb: float
├── total_hbm_gb: float
├── nvlink_bandwidth_tbps: Optional[float]
└── system_memory_tb: Optional[float]

StorageProfile (Dataclass)
├── name: str
├── type: str
├── iops_read: int
├── iops_write: int
├── throughput_read_gbps: float
├── throughput_write_gbps: float
├── latency_read_ms_p50: float
├── latency_read_ms_p99: float
├── latency_write_ms_p50: float
└── latency_write_ms_p99: float

SizingResult (Dataclass)
├── [Inputs]
│   ├── model: Model
│   ├── server: Server
│   ├── storage: StorageProfile
│   ├── concurrency: int
│   ├── effective_context: int
│   ├── kv_precision: str
│   ├── kv_budget_ratio: float
│   ├── runtime_overhead_gib: float
│   ├── peak_headroom_ratio: float
│   └── ha_mode: str
├── [Outputs]
│   ├── kv_per_session_gib: float
│   ├── kv_total_tib: float
│   ├── sessions_per_node: int
│   ├── nodes_minimum: int
│   ├── nodes_with_headroom: int
│   └── nodes_final: int
└── warnings: List[str]
```

---

## 🔄 CICLO DE VIDA DE UMA REQUISIÇÃO

```
1. PARSE CLI
   ├── Validar argumentos obrigatórios
   ├── Aplicar defaults
   └── Construir configuração

2. CARREGAR DADOS
   ├── Abrir models.json
   ├── Abrir servers.json
   ├── Abrir storage.json
   └── Parse para dataclasses

3. VALIDAR SELEÇÕES
   ├── Modelo existe?
   ├── Servidor existe?
   └── Storage existe?

4. CALCULAR
   ├── KV cache por sessão
   ├── Sessões por nó
   └── Nós necessários

5. VALIDAR E AVISAR
   ├── Context overflow?
   ├── Precisão alta?
   ├── Storage adequado?
   └── Budget suficiente?

6. FORMATAR
   ├── Criar relatório texto
   ├── Criar JSON output
   └── Imprimir (stdout)

7. EXIT
   └── Código 0 (sucesso)
```

---

## 🧪 FLUXO DE TESTES

```
┌─────────────────────────────────────────────────────────────────┐
│                    test_sizing.py                               │
└─────────────────────────────────────────────────────────────────┘

Para cada teste:
   │
   ├─ Definir cenário (args)
   │
   ├─ Executar sizing.py --json-only
   │
   ├─ Parse JSON output
   │
   ├─ Validar resultados
   │   ├─ Nós esperados?
   │   ├─ Avisos corretos?
   │   └─ Valores dentro do range?
   │
   └─ Reportar (PASSOU/FALHOU)

Resumo final:
   ├─ Testes passados
   ├─ Testes falhos
   └─ Taxa de sucesso (%)
```

---

## 🎨 FUNÇÕES PURAS (Core Logic)

```
┌─────────────────────────────────────────────────────────────────┐
│                     FUNÇÕES PURAS                               │
│                  (Testáveis e Reutilizáveis)                    │
└─────────────────────────────────────────────────────────────────┘

calc_kv_per_session_gib(model, effective_context, kv_precision)
   ├─ Input: Model, int, str
   ├─ Output: (float, List[str])
   └─ Side effects: Nenhum

calc_sessions_per_node(server, kv_per_session_gib, kv_budget_ratio, 
                       runtime_overhead_gib)
   ├─ Input: Server, float, float, float
   ├─ Output: int
   └─ Side effects: Nenhum

calc_nodes_required(concurrency, sessions_per_node, peak_headroom_ratio,
                    ha_mode)
   ├─ Input: int, int, float, str
   ├─ Output: (int, int, int)
   └─ Side effects: Nenhum

generate_storage_warnings(storage, model, effective_context, server)
   ├─ Input: StorageProfile, Model, int, Server
   ├─ Output: List[str]
   └─ Side effects: Nenhum
```

---

## 📈 ESCALA DE VALORES

```
┌─────────────────────────────────────────────────────────────────┐
│                    RANGES TÍPICOS                               │
└─────────────────────────────────────────────────────────────────┘

Concurrency:
   Low:    10 - 100 sessões
   Medium: 100 - 1.000 sessões
   High:   1.000 - 10.000 sessões
   Extreme: > 10.000 sessões

Effective Context:
   Short:  4k - 16k tokens
   Medium: 16k - 64k tokens
   Long:   64k - 131k tokens
   Extreme: > 131k tokens (clamped)

KV per Session:
   Small:  0.05 - 0.5 GiB
   Medium: 0.5 - 2.0 GiB
   Large:  2.0 - 5.0 GiB
   Extreme: > 5.0 GiB

Sessions per Node:
   Low:    < 100
   Medium: 100 - 1.000
   High:   1.000 - 5.000
   Extreme: > 5.000

Nodes Required:
   Small:  1 - 2 nós
   Medium: 3 - 10 nós
   Large:  10 - 50 nós
   Extreme: > 50 nós
```

---

**Versão:** 1.0  
**Data:** 2026-02-07
